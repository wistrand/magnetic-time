# magnetic-time

A desktop clock where the hands carry magnets and move beneath a thin layer of
liquid filled with magnetic particles. The particles are dragged along by the
field of the hands, lagging with viscous drag: the hour hand carries a tight
cluster, the second hand outruns its particles and pulls them into chains and
comet shapes that slowly relax.

Native Rust application rendered with egui.

## Status

Planning. No code yet. See [agent_docs/plan.md](agent_docs/plan.md) for the
build plan.

## Quick start (once built)

```bash
cargo run --release
```
